<UserControl x:Class="POS.Register.Features.Sales.Screens.SalesScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lib="clr-namespace:POS.Register.Lib">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <lib:InverseBoolToVisibilityConverter x:Key="InverseBoolToVis" />
    </UserControl.Resources>
    <Grid Margin="16">
        <Border Style="{StaticResource Card}" Padding="0" VerticalAlignment="Top">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Border BorderBrush="{StaticResource Line}" BorderThickness="0,0,0,1" Padding="22,18">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Style="{StaticResource TitleSection}" FontSize="22" FontWeight="Bold" Text="Today's sales" />
                        <TextBlock Style="{StaticResource BodyText}" FontSize="12" Foreground="{StaticResource Ink3}"
                                   Text="{Binding TxCount}" VerticalAlignment="Bottom" Margin="12,0,0,2" />
                    </StackPanel>
                </Border>
                <Border Grid.Row="1" BorderBrush="{StaticResource Line}" BorderThickness="0,0,0,1" Padding="22,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="210" />
                            <ColumnDefinition Width="110" />
                            <ColumnDefinition Width="90" />
                            <ColumnDefinition Width="260" />
                            <ColumnDefinition Width="140" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Style="{StaticResource Caption}" Text="RECEIPT NO." />
                        <TextBlock Grid.Column="1" Style="{StaticResource Caption}" Text="TIME" />
                        <TextBlock Grid.Column="2" Style="{StaticResource Caption}" Text="ITEMS" />
                        <TextBlock Grid.Column="3" Style="{StaticResource Caption}" Text="PAYMENT" />
                        <TextBlock Grid.Column="4" Style="{StaticResource Caption}" Text="TOTAL" HorizontalAlignment="Right" />
                        <TextBlock Grid.Column="5" Style="{StaticResource Caption}" Text="STATUS" HorizontalAlignment="Right" />
                    </Grid>
                </Border>
                <TextBlock Grid.Row="2" Style="{StaticResource BodyText}" Foreground="{StaticResource Ink3}"
                           Text="No sales yet today" HorizontalAlignment="Center" Margin="0,48"
                           Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVis}}" />
                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Rows}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.OpenDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="RowBg" Background="Transparent" BorderBrush="{StaticResource Canvas}"
                                                    BorderThickness="0,0,0,1" Padding="22,13">
                                                <ContentPresenter>
                                                    <ContentPresenter.Resources>
                                                        <Style TargetType="TextBlock" />
                                                    </ContentPresenter.Resources>
                                                </ContentPresenter>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="RowBg" Property="Background" Value="{StaticResource Surface2}" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="210" />
                                            <ColumnDefinition Width="110" />
                                            <ColumnDefinition Width="90" />
                                            <ColumnDefinition Width="260" />
                                            <ColumnDefinition Width="140" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Style="{StaticResource BodyText}" FontWeight="Bold" Text="{Binding Receipt}" />
                                        <TextBlock Grid.Column="1" Style="{StaticResource BodyText}" Foreground="{StaticResource Ink2}" Text="{Binding Time}" />
                                        <TextBlock Grid.Column="2" Style="{StaticResource BodyText}" Foreground="{StaticResource Ink2}" Text="{Binding Items}" />
                                        <StackPanel Grid.Column="3" Orientation="Horizontal">
                                            <TextBlock Style="{StaticResource BodyText}" FontWeight="SemiBold" Text="{Binding Payment}"
                                                       Visibility="{Binding IsUtang, Converter={StaticResource InverseBoolToVis}}" />
                                            <Border Background="{StaticResource GoldSoft}" BorderBrush="{StaticResource Gold}"
                                                    BorderThickness="1" CornerRadius="11" Padding="10,3" VerticalAlignment="Center"
                                                    Visibility="{Binding IsUtang, Converter={StaticResource BoolToVis}}">
                                                <TextBlock Style="{StaticResource BodyText}" FontSize="12" FontWeight="Bold"
                                                           Foreground="{StaticResource Gold}" Text="{Binding Payment}" />
                                            </Border>
                                        </StackPanel>
                                        <TextBlock Grid.Column="4" Style="{StaticResource TitleSection}" FontSize="15"
                                                   Text="{Binding Amount, StringFormat=₱{0:N2}}" HorizontalAlignment="Right" />
                                        <Border Grid.Column="5" CornerRadius="11" Padding="10,3" HorizontalAlignment="Right" VerticalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="{StaticResource ConfirmSoft}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Refunded}" Value="True">
                                                            <Setter Property="Background" Value="{StaticResource RedSoft}" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock FontSize="12" FontWeight="Bold" FontFamily="{StaticResource FontUI}" Text="{Binding Status}">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="{StaticResource Confirm}" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Refunded}" Value="True">
                                                                <Setter Property="Foreground" Value="{StaticResource Red}" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </Grid>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
